#include <stdio.h>

int main() {
    int x,y;
    printf("the amount of money chef has: ");
    scanf("%d", &x);
    printf("the price of one chocolate is: ");
    scanf("%d", &y);
    printf("the amount of chocolates the chef can buy is: %d",x/y);
    
    return 0;
}
